# tasks/diagrams

Categoría reservada.

Hoy todas las Diagram Tasks del proyecto viven dentro de `tasks/architecture/` (por ejemplo `TASK-ARCH-002-generate-system-context.md`), porque cada una está ligada a una vista de arquitectura concreta.

Esta carpeta queda reservada para futuras Diagram Tasks que no pertenezcan a la categoría `architecture` (por ejemplo, roadmaps o timelines de producto que no sean vistas arquitectónicas).

Ver [`tasks/README.md`](../README.md) para la convención completa.
